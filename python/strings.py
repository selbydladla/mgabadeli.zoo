#advanced concepts - strings

message = 'SEL,BY'

print(message.strip()) # remove keading and trailing whitespace
print(message.lower()) # convert all characters to lowercases
print(message.split(',')) # split the string into a list basedon comma 